<openerp>
<data>
    <record id="mail_template_send_to_verticals" model="mail.template">
                        <field name="name" >HR Vertical Managers Timeout</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A Quotation document with Source document (${ctx.get('origin')}) has not been approved by (${ctx.get('saleperson')}) </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'sale.order')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear HR,</p>
                                <p>A New Quotation with Source document (${ctx.get('origin')}) has not been approved by (${ctx.get('saleperson')}). </p>
                                
                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>



    <record id="mail_template_send_to_SO_approval" model="mail.template">
                        <field name="name" >HR Sales Managers Timeout</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A Sales Order document with Source document (${ctx.get('origin')}) has not been approved by (${ctx.get('saleperson')}) </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'sale.order')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear HR,</p>
                                <p>A New Sales Order with source document (${ctx.get('origin')}) has not been approved by Sales Managers </p>
                            
                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>


	    <record id="mail_template_send_to_store" model="mail.template">
                        <field name="name" >HR Store Managers Timeout</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A Delivery order with source document (${ctx.get('origin')}) has not been approved by (${ctx.get('saleperson')}) </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'stock.picking')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear HR,</p>
                                <p>A delivery order with Source document (${ctx.get('origin')}) has not been approved by (${ctx.get('saleperson')}). </p>
                                
                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>



    <record id="mail_template_send_todraft_po" model="mail.template">
                        <field name="name">HR draft PO Timeout</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A  delivery order awating purchase with Source document (${ctx.get('origin')}) has not been worked on by (${ctx.get('saleperson')}) </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'sale.order')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear HR,</p>
                                <p>A New delivery order awaiting purchase with source document (${ctx.get('origin')}) has not been approved by Purchase Managers </p>
                                
                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>





	<record id="mail_template_quotation_conversion" model="mail.template">
                        <field name="name" >HR Sales Managers Timeout</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A Quotation document with Source document (${ctx.get('origin')}) has not been converted to Sales order by the sales managers </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'sale.order')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear HR,</p>
                                <p>A Quotation with source document (${ctx.get('origin')}) has not beent converted to Sales Order by Sales Managers </p>
                                
                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>


		 <record id="mail_templ_sale_hold" model="mail.template">
                        <field name="name" >Sales Order Document On Hold</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('disapprove_person_email')}</field>
                        <field name="subject">The Sales Order Document (${object.name}) has been Put On Hold</field>
                        <field name="email_to">${ctx.get('notify_person_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'sale.order')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear ${ctx.get('notify_person_name')},</p>
                                <p>The Sales Order Document (${object.name}) has been put on hold by ${ctx.get('disapprove_person_name')}</p>
                                <p><b>Reason for withelding Sales Order</b></p>
                                <p>${ctx.get('reason_for_dispproval')}</p>
                               
                                <p>Regards and Thanks</p>
                                <p>This is an auto generated message</p>
                                ]]>
                        </field>
                </record>





    <record id="mail_template_delivery_email" model="mail.template">
                        <field name="name">Delivery Notification</field>
                        <field name="email_from">${ctx.get('system_email')}</field>
                        <field name="reply_to">${ctx.get('system_email')}</field>
                        <field name="subject">A  delivery order awating purchase with Source document (${ctx.get('origin')}) has been delivered successfully </field>
                        <field name="email_to">${ctx.get('hr_email')}</field>
                        <field name="model_id"   model="ir.model"  search="[('model', '=', 'stock.picking')]" />
                    <field name="auto_delete" eval="True"/>
                        <field name="body_html"  >
                                <![CDATA[
                                <p>Dear Operator,</p>
                                <p>A New delivery order awaiting purchase with source document (${ctx.get('origin')}) has been delivered successfully </p>

                                <p>Regards and Thanks</p>
                                <p>This is an autogenerated message</p>
                                ]]>
                        </field>
                </record>



</data>



</openerp>
